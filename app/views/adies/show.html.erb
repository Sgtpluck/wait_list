<%= session[:adie] %>

<div>
  <%= link_to "Add Twitter account!", '/auth/twitter', title: "Twitter", class: 'circular ui small twitter button' %>
</div>

<%= render 'reset' %>
<br>
Problems: <br><br>
<% @adie.problems.where(helped: 'helped').each_with_index do |problem, n| %>
  <%= n + 1 %>. <%= problem.description %>
<!-- 
  <#%= link_to problem.description, problem_path(problem.id) %> 
  -->
<br>
  <% end %>
<br>
